import React from 'react';
import FiltersList from "../FiltersList";
import Sort from "../Sort";

const AdvisorsControlPanel = () => {
    return (
        <div>
            <FiltersList />
            <Sort />
        </div>
    );
};

export default AdvisorsControlPanel;